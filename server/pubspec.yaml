name: signaling_server
description: WebSocket signaling server for P2P Messenger
version: 1.0.0

environment:
  sdk: ^3.0.0

dependencies:
  shelf: ^1.4.1
  shelf_web_socket: ^2.0.1
  web_socket_channel: ^3.0.2
  uuid: ^4.5.1
  args: ^2.5.0
